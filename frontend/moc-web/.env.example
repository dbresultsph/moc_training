# Backend API origin (no trailing path). The app appends /api for ASP.NET Core routes.
# Local: use the port your Moc.Api listens on (e.g. 5221 from launchSettings, or 5000).
VITE_API_BASE_URL=http://localhost:5000
